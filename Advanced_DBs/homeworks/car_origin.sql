CREATE TABLE DIM_CAR_ORIGIN (
    ORIGIN_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    MAKE VARCHAR2(100),
    COUNTRY VARCHAR2(100),
    REGION VARCHAR2(100)
);

INSERT INTO DIM_CAR_ORIGIN (MAKE, COUNTRY, REGION)
SELECT DISTINCT MAKE, COUNTRY, REGION
FROM CAR_MAKE_COUNTRY;

select count(*) from CAR_MAKE_COUNTRY;
select count(*) from DIM_CAR_ORIGIN;

-- 

select * from CAR_MAKE_COUNTRY;
select * from dim_car_origin;

commit;

select MIN(ORIGIN_ID)
FROM DIM_CAR_ORIGIN;